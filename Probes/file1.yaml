apiVersion: v1
kind: Pod
metadata:
  name: samplepod
spec:
  containers:
    - name: c1
      image: nginx
      startupProbe:
        exec:
          command:
            - cat
            - /usr/share/nginx/html/index.html
        initialDelaySeconds: 30
        failureThreshold: 2